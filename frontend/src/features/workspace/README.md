# Workspace Feature Surface

This directory is reserved for non-placeholder module workspace implementation in later FR batches.

Current state:
- shell ownership enforced by `features/shells/WorkspaceShell.tsx`
- module routes scaffolded under `app/(console)/app/servers/**/workspace/modules/[moduleId]`
- placeholder behavior centralized in `features/placeholders/ModulePlaceholderSurface.tsx`
