// Exporting structured content data
export {
  person,
  social,
  newsletter,
  home,
} from './content';

// Exporting design system & config options from Once UI Config
export {
  display,
  mailchimp,
  routes,
  protectedRoutes,
  baseURL,
  fonts,
  style,
  schema,
  sameAs,
  effects,
  dataStyle,
} from './once-ui.config';
